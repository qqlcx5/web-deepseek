安装
pnpm create vue@latest

集成 unocss @iconify-json/tabler element-plus lodash-es sass-embedded

iconify-json/tabler 是怎么引用

● 对，安装后直接用 class 就行，不需要额外配置。

原理是 uno.config.ts 里配置了 presetIcons()，它会自动扫描 node_modules/@iconify-json/* 下的图标集。使用时：

<span class="i-tabler-home"></span>
<span class="i-tabler-user"></span>

格式是 i-{图标集前缀}-{图标名}，tabler 集的前缀就是 tabler。@iconify-json/tabler 只提供数据，presetIcons 在编译时按需提取并内联成 CSS。
