# 02 - db（IndexedDB 存储模块）

> 路径: `src/utils/db.ts`
> 依赖: `types/index.ts`

---

## 任务清单

### 数据库初始化

- [ ] T02-1 定义常量 `DB_NAME = 'cherry-studio-web'`、`DB_VERSION = 1`、`STORE_NAME = 'appData'`、`RECORD_KEY = 'main'`
- [ ] T02-2 实现 `openDB()` — 懒初始化单例模式，`onupgradeneeded` 中创建 `appData` store（keyPath="id"）
- [ ] T02-2a 处理 `openDB` 的 onerror 回调

### CRUD 操作

- [ ] T02-3 实现 `saveAppData(data: AppData): Promise<void>` — `put({ id: 'main', ...data })` 原子覆盖写入
- [ ] T02-4 实现 `loadAppData(): Promise<AppData | null>` — `get('main')`，解包去掉 id 字段
- [ ] T02-5 实现 `clearAppData(): Promise<void>` — `delete('main')`

### 防御处理

- [ ] T02-6 处理 IndexedDB 不可用场景（隐私模式）— catch error 并 throw 友好提示
- [ ] T02-7 `dbInstance` 单例变量在连接关闭后重置为 null（版本变更场景）

### 验证

- [ ] T02-8 手动测试：`saveAppData` → `loadAppData` 数据一致
- [ ] T02-9 手动测试：`clearAppData` 后 `loadAppData` 返回 null
- [ ] T02-10 `tsc --noEmit` 类型检查通过
